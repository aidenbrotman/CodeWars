def duplicate_encode(word):

    for i in enumerate(list(word)):
        if len(list(word)) != len(set(list(word))):
            print("1")
    open('Users/aidenbrotman/Documents/IMG_2737.HEIC/')

duplicate_encode("bruh")